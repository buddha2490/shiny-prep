# Reproducible demo app — all data is generated in-memory by the factories in
# R/fct_sample_data.R, so there are no data artifacts to commit. These are the
# usual local-run leftovers.
.Rproj.user/
rsconnect/
tests/testthat/_snaps/*/*.new.*
*.Rproj
